# MongoDB Atlas connection string used for persistent stories, chapters, and settings.
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>/<database>

# A long, random value used to sign admin sessions.
JWT_SECRET=replace-with-a-long-random-secret
